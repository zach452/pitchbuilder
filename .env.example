# LLM Provider (set one or both; Anthropic is preferred)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Database (leave blank for local SQLite at ./data/db.sqlite)
DATABASE_URL=

# Vercel Blob (leave blank for local filesystem at ./data/uploads/)
BLOB_READ_WRITE_TOKEN=

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
